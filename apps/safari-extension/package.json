{
  "name": "@nuoma/safari-extension",
  "version": "0.39.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build.mts --allow-missing-converter",
    "build:real": "tsx scripts/build.mts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@nuoma/chrome-extension": "0.38.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.1.4"
  }
}
