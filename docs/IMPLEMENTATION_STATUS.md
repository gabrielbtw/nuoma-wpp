# Implementation Status

Fonte operacional para `/implementation`. Use os checkboxes abaixo como trilho
visual do que foi feito, ficou parcial e falta implementar.

## Feito

- [x] **V2.1-V2.4 Foundations** — Layout do monorepo, API, web, auth, contratos, SQLite/Drizzle e shell autenticado.
- [x] **V2.5 Job queue base** — `jobs`, `jobs_dead`, prioridade, retry/DLQ, heartbeat e cleanup.
- [x] **V2.5 Text sender** — `send_message` real via WhatsApp Web com runtime CDP conectado e allowlist.
- [x] **V2.5.21 IC-1 Voice sender** — Voz nativa do WhatsApp com Web Audio/getUserMedia, WAV 48kHz mono e smoke real 3s/15s/30s/120s.
- [x] **V2.5.22 IC-2 Open-chat reuse** — Caminho implementado e mantido como opt-in via `WORKER_SEND_REUSE_OPEN_CHAT_ENABLED=true`; envio real default renavega para o telefone do job antes de enviar.
- [x] **V2.5.23 Media sender base** — `send_document`, `campaign_step` document e `campaign_step` image/video anexam arquivo via CDP/file input com allowlist, nome/caption, limpeza de preview, múltiplas imagens por step e hardening contra toast falso.
- [x] **V2.5.24 External id do sender** — Texto, voz e `campaign_step` expõem `externalId` explícito quando o bubble novo é identificado após o envio.
- [x] **V2.5.18 Métricas operacionais** — `worker_state`, `system_events`, contadores de sync/job, throughput/h, failure-rate e latências de fila/execução alimentam `system.metrics` e o dashboard.
- [x] **Campaign step executor** — Steps `text`, `link`, `voice`, `document`, `image` e `video` executam com allowlist; `image` suporta `mediaAssetIds[]` para álbum; lote real de 9 arquivos foi revalidado com guarda de alvo ativa e mensagens persistidas no chat canônico.
- [x] **V2.6 Sync engine** — Observer CDP, reconcile forçado, histórico bounded, timeline com data/hora e segundos inferidos.
- [x] **V2.6.26 Profile photo capture** — Worker captura foto de perfil WA/IG via CDP, grava como media asset local deduplicado por SHA256 e vincula hash/asset a contato e conversa.
- [x] **V2.6.27 Attachment candidates** — Observer/handler capturam anexos visíveis de conversas WhatsApp como candidatos deduplicados, persistem media asset/evidência e a Inbox exibe contador/lista por conversa.
- [x] **V2.7 API surface base** — Root tRPC expõe routers principais e contratos seguros para `media`, `push`, `embed` e `streaming`.
- [x] **V2.7.31 CRM file storage** — `/api/media/upload` aceita `crmOwnerKey`/`conversationId` e grava arquivos CRM no provider `local` ou `s3` sob `/nuoma/files/crm/<phone-or-contact>/`.
- [x] **Automation engine** — Daemon API opt-in avalia novas mensagens inbound, automações `message_received`, segmento por tag/status/canal/janela 24h, aplica tag/status/reminder e cria jobs `campaign_step` com allowlist e dedupe por mensagem.
- [x] **Política produção de envio** — API e worker suportam modo `test`/`production`, allowlist canária opcional, elegibilidade por telefone/canal, rate limit persistido e auditoria `sender.send_policy.*` antes de tocar no WhatsApp.
- [x] **Dashboard de implantação** — `/` consome `system.metrics` e mostra saúde da API, sessão WhatsApp/CDP, workers, fila, DLQ, política de envio e últimos eventos críticos.
- [x] **Campanhas e automações** — Cadastro, contratos, builder visual base, tick manual, daemon de campanha opt-in, engine de automação opt-in e política de envio existem; criação pelo builder salva rascunho sem enfileirar envio.
- [x] **Campaign scheduler daemon** — Decisão operacional confirmada: scheduler periódico roda dentro da API via `API_CAMPAIGN_SCHEDULER_ENABLED`, sem processo/container separado nesta fase single-user.
- [x] **V2.9.6 Timeline glass bubbles** — Timeline da Inbox usa bubbles incoming com glass/blur e outgoing com gradiente, mantendo metadados, status, flags e inspector.
- [x] **V2.9.9 Ações por mensagem** — Cada bubble da Inbox expõe ações de copiar, responder e encaminhar; copiar usa clipboard, responder/encaminhar preparam o Composer com draft visual sem disparo automático.
- [x] **V2.9.11 Status delivery animado** — Mensagens outbound exibem clock, check, double-check e double-check azul com tooltip acessível, animação respeitando reduced motion e smoke visual dedicado.
- [x] **V2.9.13 Voice recording integrado** — Composer grava áudio via MediaRecorder/Web Audio, mostra preview com waveform/player, faz upload como media asset `voice` e enfileira `send_voice` para o worker IC-1 preparar WAV via ffprobe/ffmpeg.
- [x] **V2.9.14 Emoji picker** — Composer ganhou picker com busca, categorias, recentes locais e inserção no cursor sem disparar envio.
- [x] **V2.9.15 Quick replies salvos** — Respostas rápidas persistem em SQLite/tRPC, aparecem no Composer com busca e podem ser criadas/inseridas sem disparar envio.
- [x] **V2.9.16 Disparar automação na Inbox** — Sidebar da conversa tem dropdown com busca, filtro “só elegíveis”, preview dry-run e disparo real bloqueado pela política/allowlist do telefone.
- [x] **V2.9.17 Disparar campanha individual** — Sidebar da conversa tem dropdown de campanhas com busca, filtro “só elegíveis”, preview dry-run por conversa e disparo real bloqueado pela política/allowlist do telefone.
- [x] **V2.9.18 Optimistic update em send** — Composer insere bubble outbound local imediatamente, atualiza preview da conversa e reconcilia/falha sem esperar o `messages.send`.
- [x] **V2.9.19 Retry inline em msg falhada** — Bubbles outbound de texto falhadas exibem retry inline, reusam o body original, voltam para pending durante a nova tentativa e preservam bloqueios da allowlist.
- [x] **V2.9.20 Indicator "fila com X jobs"** — Cabeçalho da conversa exibe total de jobs ativos por conversa e split queued/claimed/running, sem depender de envio real no smoke.
- [x] **V2.9.21 Sidebar contato tabs** — Sidebar da Inbox fecha as abas Detalhes, Histórico, Tags, Notas e Lembretes com tags/notes persistidos no contato e histórico operacional agrupado.
- [x] **V2.9.24 Notas markdown lite preview live** — Aba Notas renderiza preview markdown lite ao vivo sem HTML bruto, salva o markdown em `contacts.notes` e valida heading/lista/link/código por smoke.
- [x] **V2.9.2 Lista virtualizada da Inbox** — Coluna de conversas usa `@tanstack/react-virtual` com limite único de 500 conversas, keys estáveis, busca/filtro preservados, reset de scroll por filtro e smoke visual/a11y dedicado.
- [x] **V2.9.5 Avatar real** — API serve `media_assets` autenticados por cookie, ConversationList/Sidebar usam foto WA/IG cacheada quando há `profilePhotoMediaAssetId` e mantêm fallback por iniciais.
- [x] **V2.9.7 Sticky date dividers** — Timeline agrupa mensagens por dia com divisores sticky, contagem por data e atributos de diagnóstico para smoke.
- [x] **V2.9.8 Media glass cards** — Bubbles renderizam imagem, vídeo, áudio/voz e documento como card glass com metadata, player/preview quando há asset e link autenticado do arquivo.
- [x] **V2.9.10 Read receipts visíveis** — Mensagens outbound mostram pill de leitura/entrega com origem `observer` quando `raw` expõe receipt e fallback pelo status persistido.
- [x] **V2.9.12 Composer 4 ações** — Composer fecha foto, vídeo, áudio e documento: upload CRM por conversa, `send_media` para imagem/vídeo, `send_document` para doc e voice recorder IC-1 para áudio.
- [x] **V2.9.22 Edição inline contato** — Aba Detalhes permite editar nome, telefone, email e Instagram sem sair da Inbox, usando `contacts.update`.
- [x] **V2.9.23 Reordenação de tags** — Tags ativas são arrastáveis na sidebar; `contact_tags.sort_order` persiste a ordem por contato via migration `0007_contact_tag_order`.
- [x] **V2.9.25 Reminders pequenos** — `reminders` ganhou router tRPC `list/create/update/complete`, listagem por conversa e aba “Lembretes” na sidebar da Inbox com texto + data/hora.
- [x] **V2.9.26 Filter chips top da timeline** — Timeline filtra por data (`todas/hoje/ontem`), tipo de mensagem e “com mídia”, com contagem visível.
- [x] **V2.9.27 Search dentro da conversa cmd+f** — Busca interna da conversa abre por `cmd/ctrl+f`, filtra a timeline e mostra contador.
- [x] **V2.9.28 Atalhos inbox** — `j/k` navegam conversas, `r` prepara reply, `e` abre cópia editável no Composer sem alterar a mensagem sincronizada e `Esc` fecha detalhe/draft/seleção.
- [x] **V2.9.29 Ressincronizar conversa** — Botão “Ressincronizar” chama `conversations.forceSync` e enfileira `sync_conversation` sem criar envio.
- [x] **V2.9.30 Tests E2E inbox flow completo** — Smoke E2E cobre filtros, busca, atalhos, edição, ressync, a11y e prints obrigatórios app + WPP.
- [x] **V2.10.1-V2.10.5 Campaigns UX** — `/campaigns` tem builder wizard glass, CSV preview/validação, tick dry-run revalidado, recipients virtualizados e workflow viewer com GSAP.
- [x] **V2.10.6 Per-step stats** — Campanhas mostram estatísticas por step com conclusão, falha, current/awaiting, navegação/reuso, eventos e último evento.
- [x] **V2.10.7 A/B variants** — Campanhas suportam `metadata.abVariants`, atribuição determinística, override de step por variante, dry-run com `variantId` e painel de performance por variante.
- [x] **V2.10.8 Evergreen auto-avaliação** — Scheduler avalia contatos em campanhas evergreen, respeita segmento/dedupe, cria recipients em execução real e expõe métricas de dry-run na UI.
- [x] **V2.10.9 Pause/resume campanha** — Smoke real revalidado com envio canário, print app + WPP e trava de destino para contato salvo no WhatsApp.
- [x] **V2.10.10-V2.10.13 Audit/IC-2 campanha** — Scheduler agrupa steps próximos por recipient/phone, payload carrega `temporaryMessages` 24h/90d, worker registra auditoria por destinatário/step, timeline exibe batch/reuso/restauração e smoke visual validou app + WPP sem criar envio.
- [x] **V2.10.16-V2.10.34 Flow builders automação/chatbot** — Automations builder tem templates, condition builder AND/OR, drag/drop de ações, preview, action registry com delay/branch/notify/trigger e execução com branch target; `/chatbots` tem rule builder, regex tester, prioridade drag/drop, fallback, variantes, tag/status/notify/automation-trigger e dry-run escopado por chatbot.
- [x] **V2.10.35 Chatbot A/B test rules** — Regras de chatbot aceitam `metadata.abTest`, dry-run resolve variante determinística sem criar job e `/chatbots` mostra painel A/B com print app + WPP.
- [x] **V2.11.1-V2.11.7 Embed overlay FAB + painel + telefone + API** — Worker injeta overlay no WhatsApp Web via CDP, isolado em Shadow DOM, com tokens Liquid Glass compilados, FAB acessível no header da conversa, painel slide-in com resumo/automações/notas, detecção de telefone por observer/DOM e ponte `window.__nuomaApi` via `Runtime.addBinding`; smoke visual validou fixture + WhatsApp Web real sem envio.
- [x] **M29 Sync top5 conversas** — Smoke operacional sincroniza histórico das 5 primeiras conversas sincronizáveis da Inbox, valida jobs/eventos e salva prints app + WPP.

## Parcial

- [x] **Deploy servidor** — Runbook hosted definido com SQLite/profile persistentes, `.env.hosted.example`, QR via CDP screenshot/túnel SSH, smoke, backup e recuperação de sessão; smoke hospedado real validado no servidor.
- [~] **V2.7 API surface** — API surface principal fechada e testada; `conversations.listUnified` segue parcial porque IG unificada fica na trilha separada de integrações.

## Falta

- [x] **Smoke hosted real** — Servidor `3.149.108.173` validado com sessão WhatsApp logada, API/Web/Worker/Chromium ativos, política canária `5531982066263` e envio real concluído.

## Execuções recentes

- 2026-05-05 — V2.9 final fechado: avatar real autenticado por `mediaAssetId`, sticky dividers por data, cards de mídia para imagem/vídeo/audio/documento, read receipt visível com fonte observer/status, Composer com foto/vídeo/áudio/doc, edição inline de contato e drag-drop de tags com `contact_tags.sort_order`. Também foi adicionado job `send_media` para imagem/vídeo no contrato/worker/API e rota `GET /api/media/assets/:id` autenticada. Validação: `npm run typecheck`, `npm run build`, `npm run test --workspace @nuoma/db -- src/repositories.test.ts`, `npm test --workspace @nuoma/api -- messages-send-voice.test.ts`, `npm run lint --workspace @nuoma/db`, `npm run lint --workspace @nuoma/api`, `npm run lint --workspace @nuoma/worker`, `npm run lint --workspace @nuoma/web` e `npm run test:v29-inbox-e2e` (`messages=4`, `syncJobsDelta=1`, `sendJobsDelta=0`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa.
- 2026-05-05 — V2.11.7 fechado M35: o worker/CDP registra `Runtime.addBinding` para `window.__nuomaApi`, o overlay captura o binding nativo e expõe uma ponte promise-based com `request`, `ping`, `contactSummary` e `refreshContact`, resolvendo respostas por `window.__nuomaApiResolve(...)`. O painel agora mostra `Ponte API` e usa `contactSummary` para hidratar o resumo do contato sem cookie/API HTTP do WhatsApp e sem envio real. O smoke foi endurecido para recriar o binding CDP quando smokes anteriores limpam `window.__nuomaApi`, preservando a sessão do WhatsApp Web sem QR. Validação: `npm run typecheck --workspace @nuoma/worker`, `npm run test --workspace @nuoma/worker -- src/features/overlay/inject.test.ts`, `npm run test:v211-overlay-api`, `npm run test:v211-overlay-fab`, `npm run test:v211-overlay-panel` e `npm run test:v211-overlay-phone`; resultado M35 `fixtureApi=online`, `fixtureMethod=contactSummary`, `fixturePanel=1`, `fixtureBlocking=0`, `wppApi=online`, `wppMethod=contactSummary`, `wppPanel=1`, `wppPhone=5531982066263`, `sendJobsDelta=0`, prints `data/v211-overlay-api-m35-fixture.png`, `data/v211-overlay-api-m35-wpp.png` e `data/v211-overlay-api-m35-app.png` (`wppMode=cdp-binding`, `ig=nao_aplicavel`). Sem envio real nesta etapa.
- 2026-05-05 — M35.1 fechado: requests do overlay via `window.__nuomaApi` agora gravam `system_events` com `type=overlay.api.request`, `method`, `phone`, `phoneSource`, `latencyMs`, `ok/error`, `errorCode`, `requestedAt` e `auditedAtUtc`. A auditoria é best-effort e não bloqueia a resposta do overlay se o log falhar.
- 2026-05-05 — V2.14 hardening mínimo: `docker-compose.yml` ganhou `restart: unless-stopped`, limites de memória (`api=512m`, `web=256m`, `worker=1800m`, `caddy=128m`), healthchecks para API/Web/Caddy e `depends_on` por `service_healthy`. `infra/scripts/deploy.sh` agora roda `npm test` no pre-deploy, sobe Compose remotamente por padrão e valida `/health` pós-deploy antes de concluir; o runbook hosted documenta o fluxo e o override `NUOMA_V2_REMOTE_COMPOSE_UP=false`.
- 2026-05-05 — M35.2 fechado: a ponte `window.__nuomaApi` passou a separar métodos de leitura (`ping`, `contactSummary`) de métodos sensíveis futuros. Chamadas de escrita diretas são bloqueadas com `mutation_guard_required`; o fluxo seguro exige `prepareMutation(...)` + `confirmMutation(...)`, inclui `nonce`, `idempotencyKey`, confirmação textual e fila sequencial no overlay antes de enviar ao worker. O worker/CDP valida a guarda antes de tratar métodos não-read-only e audita `hasMutationGuard`, `idempotencyKey`, `mutationNonce` e `mutationConfirmed` em `system_events`.
- 2026-05-05 — V2.11.6 fechado M34: o overlay passou a detectar telefone por fontes hierarquizadas do DOM (`header-title`, `url-phone`, `message-data-id`, `sidebar-active`) e expor `phoneSource` em `data-nuoma-phone-source`, evento do FAB, `window.__nuomaOverlayRefresh()` e snapshot hidratado pelo worker/CDP. A normalização agora rejeita sequências concatenadas longas e prioriza candidatos BR com `55`, evitando falso positivo em previews da sidebar com timestamps/IDs de smoke. Validação: `npm run typecheck --workspace @nuoma/worker`, `npm run test --workspace @nuoma/worker -- src/features/overlay/inject.test.ts`, `npm run test:v211-overlay-phone`, `npm run test:v211-overlay-fab` e `npm run test:v211-overlay-panel`; resultado M34 `fixturePhone=5531982066263`, `fixtureSource=message-data-id`, `fixturePanel=1`, `fixtureBlocking=0`, `wppPhone=5531982066263`, `wppSource=sidebar-active`, `wppPanel=1`, `sendJobsDelta=0`, prints `data/v211-overlay-phone-m34-fixture.png`, `data/v211-overlay-phone-m34-wpp.png` e `data/v211-overlay-phone-m34-app.png` (`wppMode=cdp`, `ig=nao_aplicavel`). Sem envio real nesta etapa.
- 2026-05-05 — M34 melhorias futuras para o próximo round: M34.1 persistir fonte/confianca/ultimo visto da detecção (`phoneSource`, confidence, lastSeenAt) para auditar mudanças do DOM do WhatsApp; M34.2 adicionar fallback seguro via detalhes do contato quando header, URL, mensagens e sidebar não expõem telefone em contato salvo.
- 2026-05-05 — V2.11.5 fechado M33: o overlay ganhou painel slide-in dentro do Shadow DOM, abrindo pelo FAB e renderizando resumo do contato, conversas, últimas mensagens, automações elegíveis e notas. Foi adicionado `window.__nuomaOverlaySetData(...)` para hidratação segura e o worker/CDP passou a montar snapshot local via SQLite (`contacts`, `conversations`, `messages`, `automations`) quando identifica o telefone do chat, sem depender de cookies cross-site do WhatsApp. Validação: `npm run typecheck --workspace @nuoma/worker`, `npm run test --workspace @nuoma/worker -- src/features/overlay/inject.test.ts` e `npm run test:v211-overlay-panel`; resultado `fixturePanel=1`, `fixtureSections=4`, `fixtureBlocking=0`, `wppPanel=1`, `wppSections=4`, `summary=1`, `automations=1`, `notes=1`, `sendJobsDelta=0`, prints `data/v211-overlay-panel-m33-fixture.png` e `data/v211-overlay-panel-m33-wpp.png` (`wppMode=cdp`, `ig=nao_aplicavel`). Sem envio real nesta etapa.
- 2026-05-05 — M33 melhorias futuras para o próximo round: M33.1 fazer o painel consumir a ponte `window.__nuomaApi` de V2.11.7 para refresh sob demanda e mutações seguras; M33.2 adicionar estados reais de loading/erro/sem contato e ação de vincular/criar contato quando o telefone não existir no CRM.
- 2026-05-05 — V2.11.4 fechado M32: criado `apps/worker/src/features/overlay/inject.ts` com `createNuomaOverlayScript()`, Shadow DOM idempotente, atributos `data-nuoma-*`, tokens Liquid Glass compilados e FAB "N" acessível (`aria-label="Abrir Nuoma CRM"`) montado dentro de `#main header`. `startSyncEngine` passou a registrar o script em `Page.addScriptToEvaluateOnNewDocument` e reinjetar após navegação de conversa. Validação: `npm run typecheck --workspace @nuoma/worker`, `npm run test --workspace @nuoma/worker -- src/features/overlay/inject.test.ts` e `npm run test:v211-overlay-fab`; resultado `fixtureMounted=1`, `wppMounted=1`, `sendJobsDelta=0`, `fixtureBlocking=0`, prints `data/v211-overlay-fab-m32-fixture.png` e `data/v211-overlay-fab-m32-wpp.png` (`wppMode=cdp`, `ig=nao_aplicavel`). Sem envio real nesta etapa.
- 2026-05-05 — M32 melhorias futuras para o próximo round: M32.1 trocar a posição fixa do FAB por detector de actions nativas do header, evitando sobreposição se o WhatsApp mudar os ícones; M32.2 conectar o clique do FAB ao painel slide-in real de `V2.11.5`, usando `embed.contactSummary`, automações elegíveis e notas por telefone.
- 2026-05-05 — V2.10.16-V2.10.34 fechados M31: `AutomationAction` ganhou `id` opcional para suporte de branch target; `triggerAutomationForPhone` executa `delay`, salto de `branch.targetActionId` com guarda anti-loop, `notify_attendant` auditável e `trigger_automation` com guarda anti-recursão. `/automations` ganhou drag/drop real nas ações, template gallery, condition builder AND/OR e preview do flow; `/chatbots` ganhou dry-run escopado por chatbot, rule builder, regex tester, prioridade drag/drop, fallback e ações `apply_tag`, `set_status`, `notify_attendant`, `trigger_automation`. Validação: `npm run typecheck --workspace @nuoma/contracts`, `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm run typecheck --workspace @nuoma/worker`, `npm test --workspace @nuoma/api -- automation-engine-daemon.test.ts` e `npm run test:v210-flow-builders` com prints `data/v210-flow-builders-m31-automations-app.png`, `data/v210-flow-builders-m31-chatbots-app.png` e `data/v210-flow-builders-m31-wpp.png` (`sendJobsDelta=0`, `blocking=0`, `ig=nao_aplicavel`). Sem envio real nesta etapa.
- 2026-05-05 — M31 melhorias futuras para o próximo round: M31.1 transformar o builder de automações em canvas com ligações visuais entre ações/branches e edição de `targetActionId` por seleção, não por texto; M31.2 persistir/exibir histórico de execução de chatbot por mensagem com ações aplicadas, variante escolhida e automação filha disparada.
- 2026-05-05 — V2.10.35 fechado M28: `chatbot_rules` ganhou `metadata_json`, contratos Zod aceitam `metadata.abTest` com variantes ponderadas e ações por variante, `chatbots.testRule` escolhe variante de forma determinística por regra/telefone/body e continua `wouldEnqueueJobs=false`. A página `/chatbots` deixou de ser placeholder e lista chatbots/regras com painel A/B e dry-run seguro; o smoke confirmou variante `controle`, `sendJobsDelta=0`, `blocking=0`, print da aplicação `data/v210-chatbot-ab-rules-m28-app.png` e print do WhatsApp Web `data/v210-chatbot-ab-rules-m28-wpp.png` (`ig=nao_aplicavel`). Validação: `npm run typecheck --workspace @nuoma/contracts`, `npm run typecheck --workspace @nuoma/db`, `npm run typecheck --workspace @nuoma/api`, `npm run build --workspace @nuoma/web`, `npm test --workspace @nuoma/api -- app.test.ts`, `npm test --workspace @nuoma/db` e `npm run test:v210-chatbot-ab-rules`. Sem envio real nesta etapa.
- 2026-05-05 — M28 melhorias futuras para o próximo round de chatbot A/B: M28.1 persistir exposição/conversão de variante por contato/mensagem para auditoria e métricas reais; M28.2 criar editor visual completo de variantes no builder de chatbot, incluindo pesos, ações por variante e promoção manual/automática de vencedora.
- 2026-05-05 — V2.10.9 fechado M27: worker limpou workers órfãos, manteve Chromium/profile aberto e revalidou envio real somente para `5531982066263` com `npm run test:v210-pause-resume`. Resultado: campaign `36`, job `78`, evento `761`, token `M27-1778005334428`, `pausedDelta=0`, `activeJobs=0`, `blocking=0`, print app `data/v210-pause-resume-m27-app.png`, print WhatsApp Web `data/v210-pause-resume-m27-wpp.png`, `wppMode=cdp-token`, `ig=nao_aplicavel`. Correção aplicada no worker: a trava de destino agora aceita o contato salvo quando a navegação recém-feita para o telefone canário abriu o composer e o WhatsApp substituiu a URL `/send?phone=` por `/`.
- 2026-05-05 — V2.10.10-V2.10.13 fechados M30: contratos aceitaram `campaignTemporaryMessagesConfig`, scheduler passou a agrupar steps próximos do mesmo recipient/phone em um batch (`scheduled_at` 0s/1s/2s no teste), job payload inclui `campaignBatch*` e `temporaryMessages` (`24h` antes, `90d` depois, `restoreOnFailure=true`), worker registra `sender.temporary_messages.audit` por destinatário/step e remove `awaitingJobIds` conforme cada step conclui. `/campaigns` mostra timeline com `batch:x/y`, `temp:...`, badges `24h`/`24h/90d`, `NAVEGOU`/`REUSO` e métricas de tempo/reuso. Validação: `npm run typecheck --workspace @nuoma/contracts`, `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/worker`, `npm run typecheck --workspace @nuoma/web`, `npm test --workspace @nuoma/api -- campaign-scheduler.test.ts` e `npm run test:v210-recipient-audit-ic2` com prints `data/v210-recipient-audit-ic2-m30-app.png` e `data/v210-recipient-audit-ic2-m30-wpp.png` (`campaignStepJobsDelta=0`, `blocking=0`, `ig=nao_aplicavel`). Observação: a automação real de clicar na UI do WhatsApp para trocar 24h/90d ficou como M30.1/M30.2 porque o seletor atual não ficou confiável; a auditoria registra `executionMode=audit_only`.
- 2026-05-05 — M30 melhorias futuras para o próximo round: M30.1 implementar navegação robusta no WhatsApp Web para alterar mensagens temporárias por chat com detecção de labels PT/EN, screenshot antes/depois e rollback obrigatório; M30.2 persistir uma tabela de auditoria materializada por recipient/job para histórico longo sem depender do limite recente de `system_events`.
- 2026-05-05 — M29 sync top5 conversas: worker reiniciado com `WORKER_JOB_LOOP_ENABLED=true` mantendo Chromium/profile aberto. O smoke `npm run test:sync-top5` leu a Inbox, pulou fixtures de smoke visíveis e enfileirou `sync_history` com `maxScrolls=25`/`delayMs=250` para as 5 primeiras conversas sincronizáveis: jobs `71..75`, conversas `32517`, `32515`, `32446`, `32439`, `32440`. Resultado: `completed=5`, `sendJobsDelta=0`, `blocking=0`, prints `data/sync-top5-m29-app.png` e `data/sync-top5-m29-wpp.png`, `ig=nao_aplicavel`. Três conversas chegaram em `top-reached`; duas terminaram `empty-window`. Uma tentativa literal anterior falhou em fixture sintética `V2.9.13 Voice Smoke` (`composer not found`), por isso o smoke passou a registrar `skipped` para fixtures de teste.
- 2026-05-05 — M29 melhorias futuras para o próximo round de sync: M29.1 separar/arquivar fixtures de smoke para não poluir a Inbox operacional nem entrar em sync real; M29.2 criar painel de bulk sync com progresso por conversa, `stoppedReason`, contagem antes/depois, falhas retryáveis e botão “sincronizar 5/10 primeiras” sem precisar rodar script.
- 2026-05-05 — V2.10.8 fechado M26: o scheduler de campanhas passou a autoavaliar campanhas `evergreen=true` em status `running/scheduled`, lendo contatos, aplicando o mesmo `segmentMatches` das automações para tag/status/canal/data, exigindo telefone para WhatsApp e deduplicando por `contactId` e telefone normalizado antes de criar `campaign_recipients`. Em dry-run ele reporta `evergreenCampaignsScanned`, `evergreenContactsScanned`, `evergreenRecipientsPlanned`, `evergreenRecipientsCreated` e `evergreenRecipientsSkipped` sem mutar banco/fila; em execução real cria recipients com `metadata.source=campaign_scheduler.evergreen`, variáveis do contato e registra `campaign.evergreen.evaluated`. A página `/campaigns` mostra métricas evergreen no último tick e painel “Evergreen auto” por campanha. Validação: `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web`, `npm test --workspace @nuoma/api -- campaign-scheduler.test.ts`, `npm test --workspace @nuoma/api -- app.test.ts` e `npm run test:v210-evergreen` com screenshots app `data/v210-evergreen-m26-app.png` e WhatsApp Web `data/v210-evergreen-m26-wpp.png` (`planned>=2`, `created=0`, `recipientsDelta=0`, `campaignStepJobsDelta=0`, `blocking=0`). Sem envio real nesta etapa.
- 2026-05-05 — M26 melhorias futuras para o próximo round de evergreen: M26.1 trocar varredura simples por cursor incremental/updatedSince e batch paginado para bases grandes, evitando reavaliar todos os contatos; M26.2 adicionar painel de elegibilidade por campanha com motivos de exclusão por contato, simulação por segmento e ação manual “adicionar elegíveis agora”.
- 2026-05-05 — V2.10.7 fechado M25: campanhas agora aceitam `metadata.abVariants` com duas ou mais variantes, pesos e `stepOverrides`; o scheduler resolve variante por recipient de forma determinística ou respeita `recipient.metadata.abVariantId`, aplica o override no step enfileirado/dry-run e grava `variantId`/`variantLabel` no payload do job. O worker carrega a variante para `sender.campaign_step.completed`, e `campaigns.list` agrega `abTest` por campanha com atribuídos, concluídos/falhas únicos, taxa, eventos e último evento por variante. A página `/campaigns` ganhou painel “A/B variants” por campanha e o builder visual salva controle/variante B no metadata sem enfileirar envio. Validação: `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/worker`, `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web`, `npm test --workspace @nuoma/api -- campaign-scheduler.test.ts`, `npm test --workspace @nuoma/api -- app.test.ts` e `npm run test:v210-ab-variants` com screenshots app `data/v210-ab-variants-m25-app.png` e WhatsApp Web `data/v210-ab-variants-m25-wpp.png` (`a=3/2/0/67`, `b=3/1/1/33`, `dryRunPhone=5531982066263`, `campaignStepJobsDelta=0`, `blocking=0`). Sem envio real nesta etapa.
- 2026-05-05 — M25 melhorias futuras para o próximo round de A/B variants: M25.1 persistir assignment em tabela própria com histórico de exposição/conversão por recipient para não depender apenas de metadata/eventos recentes; M25.2 evoluir a UI para comparar métricas por janela, editar múltiplas variantes/steps e promover uma variante vencedora automaticamente.
- 2026-05-05 — V2.10.6 fechado M24: `campaigns.list` passou a calcular `stepStats` por campanha a partir de `system_events` e recipients, agregando por `stepId` total de recipients, concluídos únicos, falhas únicas, recipients no step atual, awaiting step, taxa de conclusão/falha, eventos, navegação, reuso de chat aberto e último evento. A página `/campaigns` renderiza o painel “Per-step stats” por campanha com cards por step e atributos diagnósticos para smoke. Validação: `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web`, `npm test --workspace @nuoma/api -- app.test.ts` e `npm run test:v210-step-stats` com screenshots app `data/v210-step-stats-m24-app.png` e WhatsApp Web `data/v210-step-stats-m24-wpp.png` (`intro=3/0/75`, `proof=2/1/50`, `close=1/0/25`, `campaignStepJobsDelta=0`, `blocking=0`). Sem envio real nesta etapa.
- 2026-05-05 — M24 melhorias futuras para o próximo round de per-step stats: M24.1 persistir agregados por step em tabela/materialized snapshot para não depender do limite recente de `system_events` em campanhas longas; M24.2 adicionar drill-down por step com recipients afetados, erro resumido, job/message id e comparação por janela de tempo.
- 2026-05-05 — V2.10.1-V2.10.5 fechados M23: `/campaigns` ganhou wizard glass com abas Base/Público/Steps/Preview, templates editáveis, condition builder por step, CSV upload/cola com validação de telefone/duplicata/inválido, preview de rascunho e workflow viewer com nodes glass animados por GSAP respeitando `prefers-reduced-motion`. A listagem de campanhas agora virtualiza recipients com `@tanstack/react-virtual`, e `campaigns.list` traz até 500 recipients por campanha para a virtualização ser útil. V2.10.3 foi revalidado por dry-run: o smoke usa o canário `5531982066263`, confirma planned job visível e `campaignStepJobsDelta=0`, sem envio real. Validação: `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e `npm run test:v210-campaigns` com screenshots app `data/v210-campaigns-m23-app.png` e WhatsApp Web `data/v210-campaigns-m23-wpp.png`.
- 2026-05-05 — M23 melhorias futuras para o próximo round de campanhas: M23.1 persistir import CSV como lote revisável com mapeamento de colunas, reprocessamento e dedupe contra contatos/campanhas existentes; M23.2 evoluir o workflow viewer para editor drag/drop com zoom e preview de branches usando os mesmos dados do scheduler.
- 2026-05-05 — V2.7.31 fechado M22: criado serviço `crm-file-storage` para provider `local`/`s3`, namespace canônico `/nuoma/files/crm/<phone-or-contact>/`, normalização segura de telefone/contato, gravação local em `crm-files/nuoma/files/crm/...` e PUT S3 assinado SigV4 sem adicionar dependência pesada. A rota `/api/media/upload` preserva o fluxo legado quando não recebe dono CRM e passa a usar o storage CRM quando recebe `crmOwnerKey` ou `conversationId`; no caso de conversa, resolve telefone do thread ou cai para `contact-<id>`/`conversation-<id>`. Configuração documentada em `.env.example` com `API_CRM_STORAGE_PROVIDER=local|s3`, namespace, root local e credenciais S3. Validação: `npm test --workspace @nuoma/config`, `npm test --workspace @nuoma/api -- crm-file-storage.test.ts`, `npm test --workspace @nuoma/api -- app.test.ts`, `npm run typecheck --workspace @nuoma/api`, `npm run lint --workspace @nuoma/api` e `npm run build --workspace @nuoma/api`. Sem envio real nesta etapa.
- 2026-05-05 — M22 melhorias futuras para o próximo round de CRM storage: M22.1 adicionar job diário de mirror local→S3 com retries, checksum e política de retenção para reduzir custo de leitura/gravação em tempo real; M22.2 adicionar download/signed URL autenticado com cache local para permitir leitura de `s3://` por UI/worker sem expor bucket público.
- 2026-05-05 — Smoke completo V2 pós-M21 validado: os 22 scripts `test:v*` existentes passaram com screenshots geradas; o smoke E2E principal foi reexecutado depois do ajuste e confirmou `editMutatesPersisted=false`, `composerClearedOnCancel=true`, `syncJobsDelta=1`, `sendJobsDelta=0`, `blocking=0` e prints app + WPP (`data/v29-inbox-e2e-m21-app.png`, `data/v29-inbox-e2e-m21-wpp.png`). Desvios encontrados e corrigidos: o atalho `e` não deve chamar `messages.update` nem alterar corpo de mensagem sincronizada; agora abre apenas cópia editável no Composer, e cancelar esse rascunho limpa o texto pré-preenchido para evitar envio acidental. Validação repo-wide final: `npm run typecheck && npm run lint && npm test && npm run build` passou. Auditoria SQLite final: `activeOutside=[]` e `activeSendJobs=[]`; restaram apenas jobs ativos de sync (`sync_conversation`, `sync_history`, `sync_inbox_force`), sem envio real nesta etapa.
- 2026-05-05 — V2.9.26-V2.9.30 fechados M21: a `MessageTimeline` recebeu filter chips no topo para data (`todas`, `hoje`, `ontem`), tipo (`texto`, `imagem`, `video`, `voz`, `audio`, `doc`, `link`) e toggle `com midia`, com contador `visíveis/total` e botão `Limpar`. A busca interna ganhou test IDs e continua abrindo pelo handler `cmd/ctrl+f`; os atalhos da Inbox agora cobrem `r` para reply da mensagem selecionada/mais recente, `e` para abrir uma cópia editável no Composer sem alterar a mensagem sincronizada, e `Esc` para fechar busca/detalhe/draft/seleção conforme o contexto. A conversa também expõe `timeline-force-sync`, validando `conversations.forceSync` com job `sync_conversation`. Validação: `npm run typecheck --workspace @nuoma/db`, `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web`, `npm test --workspace @nuoma/api -- app.test.ts`, `npm run test --workspace @nuoma/db -- src/repositories.test.ts` e smoke E2E visual/a11y `npm run test:v29-inbox-e2e` com screenshots obrigatórios da aplicação `data/v29-inbox-e2e-m21-app.png` e do WhatsApp Web logado via CDP `data/v29-inbox-e2e-m21-wpp.png` (`messages=4`, `filters=image/media/yesterday/search`, `reply=reply`, `edit=edit`, `editMutatesPersisted=false`, `composerClearedOnCancel=true`, `syncJobsDelta=1`, `sendJobsDelta=0`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa.
- 2026-05-05 — M21 melhorias futuras para o próximo round do E2E da Inbox: M21.1 transformar o smoke E2E em suíte Playwright por cenário, com traces/video e fixture isolada por banco temporário; M21.2 adicionar cobertura mobile/viewport estreito e matriz de teclado com `j/k/r/e/Esc` em estados de foco diferentes.
- 2026-05-05 — V2.9.25 revalidado/fechado M20: o smoke `test:v29-reminders` agora cobre o fluxo completo da aba “Lembretes” na Inbox com fixture determinística, criação por UI, listagem aberta, validação no SQLite (`status=open`), conclusão por botão `OK`, validação no SQLite (`status=done` + `completed_at`) e garantia de que nenhum job de envio foi criado (`sendJobsDelta=0`). Durante a revalidação, foi corrigido o refresh da lista de lembretes após criar/concluir e o batching tRPC foi limitado a 4 chamadas por lote para evitar rota longa com muitos procedimentos separados por vírgula, que podia deixar a timeline com erro visual. Validação: `npm run typecheck --workspace @nuoma/db`, `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web`, `npm run test --workspace @nuoma/db -- src/repositories.test.ts`, `npm test --workspace @nuoma/api -- app.test.ts` e smoke visual/a11y `npm run test:v29-reminders` com screenshots obrigatórios da aplicação `data/v29-reminders-m20-app.png` e do WhatsApp Web logado via CDP `data/v29-reminders-m20-wpp.png` (`rows=1`, `completed=done`, `sendJobsDelta=0`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa.
- 2026-05-05 — M20 melhorias futuras para o próximo round dos reminders: M20.1 adicionar painel global de lembretes vencidos/próximos com snooze rápido, sem depender de abrir cada conversa; M20.2 criar presets de data, recorrência simples e reagendamento visual direto da sidebar.
- 2026-05-05 — V2.9.24 fechado: a aba Notas da `ContactSidebar` ganhou `MarkdownLitePreview` com renderização React segura, sem `dangerouslySetInnerHTML`, para heading `#`, `##`, `###`, parágrafo, citação, listas, checklist textual, negrito, itálico, código inline e links `http/https`. O editor continua salvando o markdown bruto em `contacts.notes`, enquanto o preview atualiza ao vivo antes de salvar. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-markdown-notes` com screenshots obrigatórios da aplicação `data/v29-markdown-notes-m19-app.png` e do WhatsApp Web logado via CDP `data/v29-markdown-notes-m19-wpp.png` (`listItems=2`, `notes=171`, `sendJobsDelta=0`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa.
- 2026-05-05 — M19 melhorias futuras para o próximo round de notas markdown: M19.1 adicionar toolbar compacta de formatação com botões para heading, checklist, negrito, link e código sem exigir digitação manual de markdown; M19.2 criar histórico/versionamento de notas por contato com autor, data, diff simples e restauração.
- 2026-05-05 — V2.9.21 fechado: a `ContactSidebar` passou a usar tabs controladas para `Detal.`, `Hist.`, `Tags`, `Notas` e `Lembr.`, mantendo os quick actions superiores e agrupando os controles de resync/histórico em uma aba própria. A aba Tags consome `tags.list` e `contacts.get`, permite aplicar/remover tags do contato vinculado e persiste via `contacts.update(tagIds)`, agora sincronizado pelo repositório com a tabela `contact_tags`. A aba Notas deixou de ser apenas estado local e salva `contacts.notes` de forma persistente, com reverter/salvar e contador. Validação: `npm run typecheck --workspace @nuoma/db`, `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/db`, `npm run lint --workspace @nuoma/web`, `npm test --workspace @nuoma/db -- src/repositories.test.ts`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-sidebar-tabs` com screenshots obrigatórios da aplicação `data/v29-sidebar-tabs-m18-app.png` e do WhatsApp Web logado via CDP `data/v29-sidebar-tabs-m18-wpp.png` (`tags=2`, `notes=37`, `sendJobsDelta=0`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa.
- 2026-05-05 — M18 melhorias futuras para o próximo round da sidebar: M18.1 transformar o indicador/abas da sidebar em superfície profunda com query param (`?sidebar=tags|notes|history`) e badges de contagem por aba; M18.2 melhorar contatos sem vínculo com ação segura de vincular/criar contato direto da sidebar, antes de liberar Tags/Notas persistidas.
- 2026-05-05 — V2.9.20 fechado: a Inbox ganhou `QueueIndicator` no cabeçalho da `MessageTimeline`, alimentado por `jobs.list` com polling de 3s quando há conversa selecionada. O cálculo filtra jobs ativos (`queued`, `claimed`, `running`) por `payload.conversationId` e mostra o total `Fila X jobs` com split `Q/C/R`, usando atributos de diagnóstico para smoke visual. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-queue-indicator` com screenshots obrigatórios da aplicação `data/v29-queue-indicator-m17-app.png` e do WhatsApp Web logado via CDP `data/v29-queue-indicator-m17-wpp.png` (`queue=3`, `queued=1`, `claimed=1`, `running=1`, `sendJobs=0`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa; o smoke criou apenas jobs sintéticos de sync e confirmou que nenhum job de envio foi criado.
- 2026-05-05 — M17 melhorias futuras para o próximo round do indicador de fila: M17.1 criar endpoint agregado `jobs.countByConversation` ou delta via SSE para evitar buscar a fila inteira no cliente e reduzir custo em bases grandes; M17.2 tornar o indicador clicável para abrir drawer filtrado da fila com IDs, tipo, agendamento, tentativas, worker atual e erro resumido.
- 2026-05-05 — V2.9.19 fechado: a `MessageTimeline` exibe botão inline `RETRY` em bubbles outbound `text` com `status=failed`, sem habilitar retry para mídia/voz ainda sem contrato. O `InboxPage` centraliza o retry com `messages.send`: mensagens otimistas falhadas voltam para `pending` durante a tentativa, mantêm contador/erro em `raw`, retornam para `falha local` quando a API bloqueia e, para mensagens falhadas vindas do servidor, o retry cria um novo bubble otimista reaproveitando o body original. A política/allowlist continua sendo aplicada pela API antes de criar job. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-retry-inline` com screenshots obrigatórios da aplicação `data/v29-retry-inline-m16-app.png` e do WhatsApp Web logado via CDP `data/v29-retry-inline-m16-wpp.png` (`sendCount=2`, `failed=failed`, `pending=pending`, `failedAgain=failed`, `jobs=0`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa; o alvo sintético fora da allowlist foi bloqueado nas duas tentativas e nenhum job foi criado.
- 2026-05-05 — M16 melhorias futuras para o próximo round do retry inline: M16.1 suportar retry de mídia/voz/documento usando o payload/media asset original e evidência por arquivo; M16.2 adicionar política de retry com limite por mensagem, cooldown visual e auditoria de tentativas no histórico da conversa.
- 2026-05-05 — V2.9.18 fechado: o `Composer` cria uma mensagem outbound local com `status=pending`, `raw.optimistic=true` e `clientMutationId`, limpa o textarea imediatamente e atualiza o cache de `conversations.list` com preview/horário antes da resposta da API. A `MessageTimeline` mescla mensagens locais com as do servidor, mostra badge `LOCAL` enquanto pendente, marca `falha local` quando a API bloqueia/erra e remove o local quando a mensagem real chega por sync/SSE por `clientMutationId` ou corpo/tipo/horário compatíveis. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-optimistic-send` com screenshots obrigatórios da aplicação `data/v29-optimistic-send-m15-app.png` e do WhatsApp Web logado via CDP `data/v29-optimistic-send-m15-wpp.png` (`optimistic=true`, `pending=pending`, `failed=failed`, `jobs=0`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa; o alvo sintético fora da allowlist foi bloqueado e nenhum job foi criado.
- 2026-05-05 — M15 melhorias futuras para o próximo round do optimistic send: M15.1 persistir `clientMutationId` no payload do job e na mensagem outbound final para reconciliação exata, sem fallback por corpo/tempo; M15.2 adicionar retry inline no bubble com `falha local`, reaproveitando o mesmo texto/draft e exibindo a causa da política/erro.
- 2026-05-05 — V2.9.17 fechado: `campaigns.listForConversation` avalia campanhas por conversa com busca, filtro elegível, canal, status executável, telefone válido e dedupe de recipient; `campaigns.execute` agora aceita `conversationId` e deriva telefone/contato/canal da conversa antes do preview ou execução segura. A Inbox ganhou o dropdown “Campanha” na sidebar com badge de bloqueio real por allowlist, botão “Prévia” dry-run e botão “Disparar” desabilitado quando o alvo não pode receber envio real. O override `allowedPhone` vindo do cliente foi fixado no canário `5531982066263`, sem permitir ampliar allowlist por payload. Validação: `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm test --workspace @nuoma/api`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-individual-campaign` com screenshots obrigatórios da aplicação `data/v29-individual-campaign-m14-app.png` e do WhatsApp Web logado via CDP `data/v29-individual-campaign-m14-wpp.png` (`options=1`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa; apenas preview dry-run em conversa sintética fora da allowlist.
- 2026-05-05 — M14 melhorias futuras para o próximo round de campanha individual: M14.1 adicionar modal de confirmação auditável antes de `dryRun=false`, mostrando destinatário, steps, mídia, política/allowlist e jobs previstos; M14.2 criar histórico por conversa dos previews/disparos de campanha com status de recipient/job, origem do operador e prevenção explícita de replay duplicado.
- 2026-05-05 — V2.9.16 fechado: `automations.listForConversation` avalia automações por conversa com busca, filtro elegível, canal, janela 24h e política de envio; `automations.trigger` agora aceita `conversationId` e deriva o telefone/canal da conversa antes do trigger seguro. A Inbox ganhou o dropdown “Automação” na sidebar com busca, switch “Mostrar só elegíveis”, badge de bloqueio real por allowlist, botão “Prévia” dry-run e botão “Disparar” desabilitado quando o alvo não pode receber envio real. Validação: `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm test --workspace @nuoma/api`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-automation-trigger` com screenshots obrigatórios da aplicação `data/v29-automation-trigger-m13-app.png` e do WhatsApp Web logado via CDP `data/v29-automation-trigger-m13-wpp.png` (`options=1`, `blocking=0`, `wppMode=cdp`). Sem envio real nesta etapa; apenas preview dry-run em conversa sintética fora da allowlist.
- 2026-05-05 — M13 melhorias futuras para o próximo round de disparo de automações: M13.1 adicionar modal de confirmação auditável antes de `dryRun=false`, mostrando jobs previstos, ações não-send e política/allowlist aplicada ao telefone; M13.2 criar histórico por conversa dos previews/disparos de automação com status dos jobs criados, origem do operador e replay seguro.
- 2026-05-05 — V2.6.27 fechado: contratos e SQLite ganharam `attachment_candidates` com vínculo para conversa, mensagem e `media_assets`; o observer WA emite `attachment-candidate-captured` para imagem, áudio/voz, vídeo e documento visíveis no DOM; o handler deduplica por conversa/mensagem/asset, registra `sync.attachment_candidate.captured` e incrementa métrica `attachmentCandidatesCaptured`. A API expõe `media.attachmentCandidatesByConversation` e a Inbox mostra o card “Anexos capturados” com total e últimos itens na sidebar. Validação: typecheck contracts/db/api/worker/web, lint db/api/worker/web, `npm test --workspace @nuoma/db -- src/repositories.test.ts`, `npm test --workspace @nuoma/worker -- sync/handler.test.ts`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v26-attachments` com screenshot obrigatório `data/v26-attachment-candidates-m12.png` (`total=4`, `blocking=0`). Sem envio real nesta etapa; print de Instagram não aplicável porque não houve envio.
- 2026-05-05 — M12 melhorias futuras para o próximo round de captura de anexos: M12.1 baixar o binário real via CDP/network e substituir `wa-visible://` por arquivo local/S3 comprimido com thumbnail autenticada; M12.2 criar galeria de mídia por conversa/álbum com filtros por tipo/origem e reconciliação de anexos vindos de resync histórico.
- 2026-05-05 — V2.9.15 fechado: adicionada tabela `quick_replies`, contratos Zod compartilhados e router tRPC `quickReplies.list/create/update/markUsed/softDelete`; o `Composer` ganhou popover de respostas rápidas salvas com busca, criação por título/atalho/categoria/texto, inserção no cursor e contador de uso sem acionar envio. Migration `0004_quick_replies` aplicada no SQLite local. Validação: typecheck/lint contracts/db/api/web, `npm test --workspace @nuoma/api -- app.test.ts -t "quick replies"`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-quick-replies` com screenshot `data/v29-quick-replies-m11.png` (`value=Olá! Posso te enviar as opções por aqui.`, `blocking=0`). Sem envio real nesta etapa; print de Instagram não aplicável.
- 2026-05-05 — M11 melhorias futuras para o próximo round das quick replies: M11.1 suportar variáveis com preview/validação por contato antes de inserir (`{{nome}}`, `{{telefone}}`, procedimento); M11.2 criar página de gestão com categorias, reordenação, import/export CSV e ranking por uso real.
- 2026-05-05 — V2.9.14 fechado: o `Composer` da Inbox ganhou picker de emoji em popover, busca por rótulos/keywords normalizados, categorias, recentes locais via `localStorage` e inserção no ponto atual do cursor sem acionar envio. O smoke cria conversa sintética, abre o picker, busca `fogo`, insere 🔥 e captura evidência visual. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-emoji-picker` com screenshot `data/v29-emoji-picker-m10.png` (`value=Teste emoji 🔥`, `blocking=0`). Sem envio real nesta etapa; print de Instagram não aplicável.
- 2026-05-05 — M10 melhorias futuras para o próximo round do emoji picker: M10.1 adicionar variações de tom de pele/long-press para gestos e rostos, persistindo a preferência do usuário; M10.2 permitir presets rápidos por atendente/campanha com ordenação por uso real no Composer.
- 2026-05-05 — V2.6.26 fechado: contratos e SQLite ganharam `profilePhotoMediaAssetId`, `profilePhotoSha256` e `profilePhotoUpdatedAt` em contatos/conversas; migration `0003_profile_photo_links` foi aplicada localmente. O worker expõe helper `__nuomaSyncProfilePhoto`, captura a imagem do header via CDP, valida hash SHA256 em Node, grava em `media-assets/<user>/profile-photos/<thread>/`, emite `profile-photo-captured`, deduplica em `media_assets` e vincula asset/hash a contato e conversa. A Inbox mostra evidência visual “Foto de perfil” com asset/hash na sidebar. Validação: typecheck contracts/db/api/worker/web, lint contracts/db/worker/web, `npm test --workspace @nuoma/worker -- sync/handler.test.ts`, `npm test --workspace @nuoma/db -- src/repositories.test.ts`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v26-profile-photo` com screenshot `data/v26-profile-photo-m9.png` (`sha=bbbbbbbbbbbb`, `blocking=0`). Sem envio real nesta etapa; print de Instagram não aplicável porque não houve envio nem fluxo IG real.
- 2026-05-05 — M9 resolvido no fechamento da V2.9: `mediaAssetId` agora é servido por rota autenticada e a Inbox troca fallback por avatar real quando o asset local ou `sourceUrl` está disponível. Recaptura periódica e histórico limitado de hashes permanecem fora da Fase 9 e entram em hardening operacional futuro.
- 2026-05-05 — V2.9.13 fechado: o `Composer` da Inbox ganhou gravação de voz integrada por `MediaRecorder` + Web Audio meter, preview com waveform/player, descarte seguro e botão `Enviar áudio`. O envio faz upload multipart em `/api/media/upload` como media asset `voice` e chama `messages.sendVoice`, que valida conversa WhatsApp + media asset `audio/voice`, aplica hard guard para permitir apenas `5531982066263` antes da política configurável da API e enfileira `send_voice` com `audioPath`, `mediaAssetId`, duração e origem `inbox.voice_recorder`; o worker IC-1 continua responsável por `ffprobe`/`ffmpeg` e voz nativa no WhatsApp. `messages.send` também passou a aplicar a mesma trava para texto WhatsApp. Validação: `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/api`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web`, `npm test --workspace @nuoma/api -- messages-send-voice.test.ts` e smoke visual/a11y `npm run test:v29-voice-recorder` com screenshot `data/v29-voice-recorder-m8.png` (`state=recorded`, `audio=true`, `blocking=0`). Sem envio real nesta etapa; print de Instagram não aplicável.
- 2026-05-05 — M8 melhorias futuras para o próximo round do voice recorder: M8.1 adicionar trim/waveform editável antes do upload, com corte de silêncio e limite visual de duração; M8.2 exibir no inspector da mensagem/job a evidência de preparo (`durationSource`, sample rate, canais, bit depth e WAV final) depois que o worker concluir o IC-1.
- 2026-05-05 — V2.9.11 fechado: `MessageTimeline` ganhou `DeliveryStatusIndicator` animado para mensagens outbound com estágios `clock`, `single-check`, `double-check` e `blue-double-check`, tooltip acessível, estado `read` azul com pulso visual e fallback para `prefers-reduced-motion`. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e smoke visual/a11y `npm run test:v29-delivery-status` com screenshot `data/v29-delivery-status-m7.png` (`pending:clock`, `sent:single-check`, `delivered:double-check`, `read:blue-double-check`, `blocking=0`). Sem envio real nesta etapa; print de Instagram não aplicável.
- 2026-05-05 — M7 melhorias futuras para o próximo round do delivery status: M7.1 persistir histórico de transição por mensagem para animar mudança real ao vivo e auditar tempo entre enviada, entregue e lida; M7.2 adicionar preferência de densidade/contraste para status em conversas longas, com modo reduzido de movimento mais explícito.
- 2026-05-05 — V2.9.6 fechado: `MessageTimeline` recebeu bubbles glass para inbound e gradiente controlado para outbound, com borda translúcida, blur, destaque de topo, cauda visual, hover sem quebrar layout e preservação dos badges, precisão de timestamp, horário com segundos, status e inspector. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e smoke visual `npm run test:v29-timeline-glass` com screenshot `data/v29-timeline-glass-m5.png` (`incomingBackdrop=blur(40px)`, `outgoingGradient=outgoing`, `blocking=0`). Sem envio real nesta etapa; print de Instagram não aplicável.
- 2026-05-05 — M5 melhorias futuras para o próximo round da timeline: M5.1 extrair tokens formais de bubble (`incomingGlass`, `outgoingGradient`, `selected`, `failed`) para o design system e manter contraste testável por tema; M5.2 criar densidade compacta/expandida por preferência do usuário, especialmente para conversas longas com áudio/mídia.
- 2026-05-05 — V2.9.9 fechado: `MessageTimeline` ganhou toolbar por mensagem com copiar, responder e encaminhar; copiar grava o texto/placeholder da mídia no clipboard com feedback, responder e encaminhar criam `MessageActionDraft` e exibem banner visual no `Composer`, sendo que encaminhar preenche o texto para revisão antes do envio. O smoke corrigiu a regressão de acessibilidade `nested-interactive` removendo a semântica de botão do container do bubble e mantendo os ícones como botões reais. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e `npm run test:v29-message-actions` com screenshot `data/v29-message-actions-m6.png` (`buttons=3`, `draft=forward`, `blocking=0`). Sem envio real nesta etapa; print de Instagram não aplicável.
- 2026-05-05 — M6 melhorias futuras para o próximo round das ações por mensagem: M6.1 adicionar seletor de destino para encaminhar para outro contato/conversa, com busca e multi-seleção segura; M6.2 implementar reply nativo no worker/CDP quando o payload `quotedMessageId` for consumido de ponta a ponta.
- 2026-05-05 — V2.9.25 fechado: `reminders` ganhou API tRPC (`list`, `create`, `update`, `complete`) usando ownership por usuário, listagem filtrada por conversa/status e conclusão com `completedAt`. A Inbox ganhou aba “Lembretes” no `ContactSidebar`, criando lembretes pequenos por texto + data/hora e mostrando itens abertos do chat. Validação: `npm run typecheck --workspace @nuoma/db`, `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm run test --workspace @nuoma/db -- src/repositories.test.ts`, `npm test --workspace @nuoma/api -- app.test.ts`, `npm run build --workspace @nuoma/web`, `npm run lint --workspace @nuoma/web` e smoke visual `npm run test:v29-reminders` com screenshot `data/v29-reminders-m4.png`.
- 2026-05-05 — M4 melhorias futuras para o próximo round dos reminders: M4.1 adicionar painel global de lembretes vencidos/próximos com snooze rápido, sem depender de abrir cada conversa; M4.2 criar presets de data (“hoje”, “amanhã”, “3 dias”, “1 semana”) e recorrência simples para follow-ups repetidos.
- 2026-05-05 — Correção visual pós-smoke Inbox: removido `whileHover` do item virtualizado da lista porque o transform do Framer Motion competia com `translateY` do `@tanstack/react-virtual` e podia empilhar conversas no hover. `test:v29-virtual-list` agora passa o mouse em várias linhas e falha se elas colapsarem (`hoverMinGap=76` no smoke atual). A Inbox também passou a exibir conversa WhatsApp só como contato salvo ou número; threads de presença/status como `visto por último...` ficam fora da listagem.
- 2026-05-05 — V2.5.18 fechado: `system.metrics` ganhou bloco `operations` com janela de 1h, throughput/h, terminal jobs, completed/failed, failure-rate, latência média de espera (`scheduled_at → claimed_at`), latência média de execução (`claimed_at → completed_at`) e pior execução. O dashboard `/` exibe o painel “Métricas operacionais”. Validação: `npm run typecheck --workspace @nuoma/db`, `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm test --workspace @nuoma/api -- app.test.ts`, `npm run build --workspace @nuoma/web` e smoke visual `npm run test:v25-metrics` com screenshot `data/v25-metrics-m3.png`.
- 2026-05-05 — M3 melhorias futuras para o próximo round das métricas: M3.1 persistir série temporal agregada por minuto/hora para tendência de throughput e failure-rate sem recalcular em cima da tabela `jobs`; M3.2 adicionar percentis p50/p95 de latência por tipo de job e por worker para separar gargalo de fila, execução e WhatsApp/CDP.
- 2026-05-05 — V2.9.2 fechado: `ConversationList` usa `@tanstack/react-virtual` com `INBOX_CONVERSATION_LIMIT=500`, `getItemKey` por `conversation.id`, `overscan=6`, reset de scroll quando busca/filtro mudam e atributos de diagnóstico `data-total-count`/`data-visible-count` para smoke. O cache realtime da V2.9.3 agora usa a mesma chave de lista. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e `npm run test:v29-virtual-list` com screenshot `data/v29-virtual-list-m2.png`.
- 2026-05-05 — M2 melhorias futuras para o próximo round da lista virtualizada: M2.1 trocar paginação por cursor/infinite loader para buscar mais de 500 conversas sem carregar tudo de uma vez; M2.2 mover navegação por teclado para usar o conjunto filtrado/virtualizado, evitando seleção invisível quando busca ou canal estiverem ativos.
- 2026-05-05 — V2.5.23 fechado formalmente: cobertura atual confirma `send_document`, `campaign_step` document, `campaign_step` image, álbum de 4 imagens por `mediaAssetIds[]` e `campaign_step` video com allowlist, caption/variáveis, metadados de mídia e evento auditável. Validação local: `npm run typecheck --workspace @nuoma/contracts`, `npm run typecheck --workspace @nuoma/worker`, `npm test --workspace @nuoma/worker -- job-loop.test.ts` e smoke visual `npm run test:v25-media` com screenshot `data/v25-media-m1.png`.
- 2026-05-05 — M1 melhorias futuras para o próximo round do sender de mídia: M1.1 persistir evidência por arquivo anexado no WhatsApp, com contagem de previews e status individual para documento/imagem/vídeo/álbum em vez de depender só do agregado `mediaCount`; M1.2 levar `mediaAssetIds[]` para o builder visual de campanha, permitindo selecionar/reordenar múltiplas imagens na UI antes de enfileirar o step.
- 2026-05-05 — Campanha image album fechada: contrato `image` aceita `mediaAssetIds[]` opcional, worker envia múltiplos arquivos pelo mesmo file input e evento registra `fileNames`, `mimeTypes` e `mediaCount`. Validação local: `npm run typecheck --workspace @nuoma/contracts`, `npm run typecheck --workspace @nuoma/worker`, `npm test --workspace @nuoma/worker -- job-loop.test.ts` (`15/15`). Smoke hosted real: job `19` enviou 4 imagens juntas para `5531982066263`, completou com `mediaCount=4`, `captionSent=true`, external id `3EB017F9D9A33390EF1D95` e política allowlist `test` aplicada.
- 2026-05-05 — Decisão operacional do campaign scheduler fechada: Opção A. O daemon periódico permanece dentro do processo da API e é ativado por `API_CAMPAIGN_SCHEDULER_ENABLED=true` quando quisermos campanhas automáticas; não haverá processo/container `scheduler` separado nesta etapa single-user.
- 2026-05-05 — Builder visual base de campanhas/automações implementado em `/campaigns` e `/automations`: campanhas criam rascunho com canal, evergreen, segmento simples e steps `text/link/voice/document/image/video`; automações criam rascunho com trigger, canal, janela 24h e ações `send_step/apply_tag/remove_tag/set_status/create_reminder`. Criação não enfileira jobs nem toca no worker. Validação: `npm run typecheck --workspace @nuoma/web`, `npm run build --workspace @nuoma/web` e `npm run lint --workspace @nuoma/web`.
- 2026-05-04 — Dashboard de implantação fechado: `system.metrics` agrega jobs por status, DLQ, workers, heartbeat, CDP/sessão WhatsApp, política de envio e eventos críticos; smoke visual em `/` passou com axe `0` violações e screenshot `data/deployment-dashboard-smoke.png`.
- 2026-05-04 — Runbook hosted fechado: `docs/runbooks/HOSTED_DEPLOYMENT.md` documenta deploy em `/srv/nuoma-wpp-v2`, volume `data/`, `.env.hosted.example`, QR via `infra/scripts/cdp-screenshot.mjs` + túnel SSH, smoke, backup/restore e recuperação de sessão sem apagar profile.
- 2026-05-04 — Smoke hosted real iniciado: deploy em `ubuntu@3.149.108.173:/srv/nuoma-wpp-v2`, Caddy público em `http://3.149.108.173:8080`, API health OK, Web OK, admin seed criado, worker `worker-hosted-1` idle com `browser_connected=1`, fila/DLQ zeradas e QR salvo em `data/hosted-whatsapp-screen.png`. Nenhum envio WhatsApp executado.
- 2026-05-04 — Smoke hosted real fechado: QR escaneado, sessão WhatsApp preservada no profile Docker, `WORKER_KEEP_BROWSER_OPEN=false` validado com limpeza automática de locks órfãos do Chromium, job `4` enviou texto real para `5531982066263`, completou em `2026-05-04 22:02 BRT`, registrou `sender.send_policy.allowed` e `sender.text_message.completed`, e sincronizou outbound `Smoke hosted real self-chat guard 2026-05-05T01:02:28.062Z`.
- 2026-05-04 — Smoke texto job `14` para `5531982066263`, outbound persistido.
- 2026-05-04 — Smoke IC-2 jobs `15` e `16`, segundo envio com `navigationMode="reused-open-chat"`.
- 2026-05-04 — Smoke IC-1 voz job `21`, fixture 3s, voice nativo entregue.
- 2026-05-04 — Smoke IC-1 voz job `23`, `/Users/gabrielbraga/Desktop/Rebote.ogg` convertido para WAV e entregue.
- 2026-05-04 — Smoke IC-1 voz jobs `24` e `25`, fixtures 30s e 120s entregues.
- 2026-05-04 — `campaign_step` validado em teste para texto com variáveis e voz via `mediaAssetId`, sem envio real.
- 2026-05-04 — Tick manual de campanha implementado na API e exposto em `/campaigns`; teste garante que não duplica job enquanto `awaitingJobId` existe.
- 2026-05-04 — Daemon periódico opt-in da API implementado com `API_CAMPAIGN_SCHEDULER_ENABLED=false` por padrão.
- 2026-05-04 — Preview `dryRun` de campanha implementado: mostra jobs planejados sem criar fila nem alterar recipients.
- 2026-05-04 — Campanha local `Smoke campanha dry-run 2026-05-04` criada para `5531982066263`; preview pelo painel mostrou 1 job planejado e SQLite confirmou 0 `campaign_step` jobs criados.
- 2026-05-04 — Smoke real de campanha confirmado: job `26` (`campaign_step` text) enviou para `5531982066263`, criou `sender.campaign_step.completed` evento `55`, persistiu message `46028`/external id `3EB0812F6028722493C5DC` e concluiu recipient `1`.
- 2026-05-04 — V2.5.24 fechado no worker: eventos de `send_message`, `send_voice` e `campaign_step` passam a receber `externalId` quando `lastExternalIdAfter` muda em relacao ao snapshot anterior.
- 2026-05-04 — V2.5.23 base de documento implementada no worker: `send_document` direto e `campaign_step` document usam media asset/local path, validam tipo `document`, renderizam `fileName`/`caption` com variáveis e registram evento auditável.
- 2026-05-04 — Smoke real de documento: job `29` no número `5531982066263` abriu fluxo de documento, mas o WhatsApp retornou toast `1 arquivo que você tentou adicionar não é compatível.` (`toast1ar`) e não criou mensagem outbound. O falso completed foi corrigido no SQLite e o worker agora ignora `externalId` `toast*`, falha se houver toast no lugar de documento e limpa previews pendurados antes de anexar.
- 2026-05-04 — Smoke real de documento job `30`: corrigido bug de seleção de input (`image/*` era aceito por engano por conter `*`; agora exige `accept="*"` exato). O PDF foi enviado para `5531982066263`, sincronizado como message `47177`/external id `3EB09E573191F5A38BEDE4`, job/recipient reconciliados para `completed` e evento `64` criado.
- 2026-05-04 — Worker endurecido para documento: após clicar em `Enviar`, aguarda reconcile por até 25s, falha imediatamente em `toast*`, aceita sucesso por novo `externalId`/nova bolha e limpa diálogo transitório `Iniciando conversa`.
- 2026-05-04 — `/campaigns` agora mostra recipients por campanha com timeline auditável baseada em `system_events`; smoke visual confirmou recipient `#2` concluído com job `30`, message `47177` e external id `3EB09E573191F5A38BEDE4`.
- 2026-05-04 — Timeline de `/campaigns` ganhou indicador `NAVEGOU`/`REUSO` baseado em `payload.navigationMode`; browser local confirmou `nav:navigated` nos eventos existentes e suporte pronto para `reused-open-chat`.
- 2026-05-04 — Cards de campanha agora exibem métricas agregadas por `system_events`: eventos, steps OK, falhas, navegações, reuso e duração observada; browser local confirmou as métricas nas campanhas de smoke.
- 2026-05-04 — Pipeline de mídia expandido para `campaign_step` image/video: contratos aceitam `image` e `video`, worker chama `sendMediaMessage` com allowlist e testes unitários cobrem ambos sem envio real.
- 2026-05-04 — Smoke real de imagem: job `31` enviou `nuoma-wpp-image-smoke-20260504.png` para `5531982066263`, completou recipient `3`, sincronizou message `51903`/external id `3EB0B66303EC5E2DB72734` e evento `65` em `/campaigns`. Durante o smoke foi corrigida a resolucao de `mediaAsset.storagePath` relativo a raiz do repo.
- 2026-05-04 — Safety correction durante lote real de mídia: usuário reportou envio para conversa errada enquanto jobs `35`-`37` usavam `navigationMode="reused-open-chat"`. Auditoria SQLite confirmou evidência em outra conversa (`553185596476`), então jobs `35`-`37` e recipients `8`-`10` foram marcados como `failed`/misrouted, job `38` foi bloqueado como `failed`, e o reuse de chat aberto passou a ficar desligado por default; antes de qualquer novo envio o worker deve renavegar para `/send?phone=...`.
- 2026-05-04 — V2.6 safety guard: handler de sync agora rejeita `conversationId`/`candidatePhone` quando o thread ativo revela outro telefone. O evento passa a ir para a conversa real observada e registra `sync.reconcile_target_mismatch`, evitando poluir a conversa canônica com mensagens do chat ativo errado.
- 2026-05-04 — Correção de dados pós-incidente: removidas da conversa canônica `28654` as cinco linhas de incidente duplicadas por `external_id` entre `10:37` e `10:39`, restaurado título `Gabriel Braga Nuoma`, com backup `data/backups/nuoma-v2-before-clean-canonical-28654-20260504-074927.db`.
- 2026-05-04 — Estabilização V2.6 ampliada: removidas mais 38 linhas de `28654` cujo `raw.thread.phone=553185596476` e `candidatePhone=5531982066263`; conversa fantasma `32421/online` arquivada porque `59/59` mensagens já existiam em outras conversas. Backup: `data/backups/nuoma-v2-before-v26-dedupe-clean-20260504-075410.db`. Após a limpeza, não há `external_id` duplicado entre conversas ativas nem conversas genéricas visíveis.
- 2026-05-04 — Handler agora também trata `online` como título genérico/untrusted. Se o reconcile traz candidate/conversation, mas o thread ativo só revela `online` sem telefone nem título útil, o evento não é roteado para a canônica e registra `sync.reconcile_target_untrusted`.
- 2026-05-04 — V2.5 sender recebeu guarda pré-envio no CDP: `send_message`, `send_voice`, `send_document` e `send_media` bloqueiam antes de tocar no composer se o chat ativo não confirmar o telefone esperado via `/send?phone=` ou título numérico. Smoke sem envio com WPP aberto em `553185596476` contra alvo `5531982066263` retornou `wouldAllow5531982066263=false`.
- 2026-05-04 — Smoke de vídeo com guarda ativa: job `39` bloqueou antes do envio por header ambíguo (`clique para mostrar os dados do contato`), sem mensagem enviada; após corrigir a extração do título do chat, job `40` enviou `17994083840269076.mp4` para `5531982066263`, completou recipient `8`, sincronizou message `56280`/external id `3EB01DDDCDE4BF927CE2A3`, `content_type=video`, sem duplicatas ativas.
- 2026-05-04 — Fechamento V2.5 do lote real de 9 arquivos para `5531982066263`: jobs válidos `46`/`47`/`48` reenviaram `A1BH.ogg`, `Preço.ogg` e `A2novo.ogg` como voice nativo com `nativeVoiceEvidence=true` e messages `58311`/`58511`/`58805`; job `40` validou vídeo; jobs `44`/`45`/`41`/`42`/`43` validaram as 5 imagens como `content_type=image`, não figurinha, com messages `57654`/`58088`/`56607`/`56725`/`56836`. Auditoria final: `queued_or_active_jobs=0`, recipients `5..13` todos `completed`, `grouped_rows=0`, duplicatas ativas `0`, eventos completed fora da allowlist `0`.
- 2026-05-04 — Correção de reconciliação de mídia: o observer passou a ignorar wrappers sintéticos `grouped-sticker--...`, e o sender só aceita `externalId` quando o `lastExternalId` muda para um ID real. Isso corrigiu o job `43` para `3EB06C5EC79EA18C4719DE` e o job `44` para `3EB07AFB18B4E040E3E5DB`, evitando classificar agrupador ou ID antigo como entrega nova.
- 2026-05-04 — Início V2.7 API surface: adicionados routers `media`, `push`, `embed` e `streaming` ao root tRPC. `media.upload` registra/deduplica por SHA256, `push.subscribe/unsubscribe/test` persiste subscription e registra teste event-only, `embed.contactSummary/eligibleAutomations/addNote` atende overlay por telefone, e `streaming.*` retorna contrato seguro enquanto screencast runtime não existe. Teste `apps/api/src/app.test.ts` cobre esses contratos.
- 2026-05-04 — V2.7 CRUD base avançou: `contacts.update` exposto com CSRF e ownership por usuário; `media.get`, `media.update` e `media.softDelete` expostos, mantendo dedupe por SHA256 e listagem filtrando deletados por padrão. Teste de integração cobre update de contato e ciclo media upload/get/update/softDelete/list.
- 2026-05-04 — V2.7 configuração operacional avançou: `tags.update/delete`, `attendants.list/update` e `automations.get/update/test` expostos. `automations.test` é seco e retorna elegibilidade sem enfileirar jobs nem tocar no worker. Teste de integração cobre esses contratos.
- 2026-05-04 — V2.7 chatbot surface avançou: `chatbots.list/get/update/listRules/updateRule/testRule` expostos. `chatbots.testRule` avalia `phone` + `body` por prioridade, permite fallback, retorna ações previstas e mantém `wouldEnqueueJobs=false`.
- 2026-05-04 — V2.7 contatos avançou: `contacts.import` aceita `csv` ou `rows`, faz `dryRun` por padrão, normaliza telefone/e-mail/Instagram e deduplica por identidade; `contacts.search` busca por nome, telefone, e-mail, Instagram e notas sem criar jobs. Índice FTS5 físico segue pendente como otimização.
- 2026-05-04 — V2.7 campanha execute seguro: `campaigns.execute` cria recipients e chama o scheduler apenas quando `dryRun=false`; por padrão é preview, e execução real passa pela política `API_SEND_POLICY_MODE`/`API_SEND_ALLOWED_PHONES`.
- 2026-05-04 — V2.7 automação manual segura: `automations.trigger` e `embed.dispatchAutomation` usam `dryRun=true` por padrão. Execução real cria jobs `campaign_step` apenas para ações `send_step` e passa pela política de envio da API; ações não executáveis ainda retornam como skipped no resultado.
- 2026-05-04 — V2.7 conversations CRUD base fechado: `conversations.create/update/softDelete/restore` expostos. O delete público arquiva logicamente via `isArchived`, preservando histórico e mensagens.
- 2026-05-04 — V2.7 messages CRUD base fechado: `messages.get/create/update/softDelete` expostos e `messages.listByConversation` ganhou `includeDeleted`. `messages.send` segue separado como endpoint de enqueue.
- 2026-05-04 — V2.7 campaigns CRUD base fechado: `campaigns.get/update/softDelete/restore` expostos. `softDelete` muda status para `archived`; `execute` e `tick` seguem como operações separadas de execução.
- 2026-05-04 — V2.7 users admin base fechado: `users.get/update/deactivate` adicionados aos contratos admin. Desativação é lógica via `isActive=false`, sem apagar usuário ou histórico.
- 2026-05-04 — V2.7 automations CRUD base fechado: `automations.softDelete/restore` adicionados. Arquivamento é lógico via status `archived`; trigger manual continua separado e protegido por `dryRun`/política de envio.
- 2026-05-04 — V2.7 chatbots CRUD base fechado: `chatbots.softDelete/restore` e `chatbots.deleteRule` adicionados. Arquivamento de chatbot usa status `archived`; delete de regra desativa via `isActive=false`.
- 2026-05-04 — V2.7 contacts.search físico fechado: adicionada migration `0002_contacts_fts` com tabela virtual FTS5 `contacts_fts`, backfill dos contatos existentes e triggers para insert/update/delete. `contacts.search` usa FTS5 com fallback LIKE para bancos ainda não migrados.
- 2026-05-04 — V2.7 media upload físico fechado: adicionada rota `/api/media/upload` com cookie auth, CSRF, multipart real via Fastify, gravação física em `media-assets` ao lado do DB e dedupe por SHA256 reaproveitando `media_assets`.
- 2026-05-04 — V2.7 push real fechado no backend: `push.test` usa `web-push` quando VAPID está configurado (`API_WEB_PUSH_VAPID_PUBLIC_KEY`/`PRIVATE_KEY`/`SUBJECT`), tenta entregar para subscriptions do usuário, remove endpoints 404/410 e mantém fallback `event-only` quando VAPID não existe.
- 2026-05-04 — V2.7 tests fechados para o escopo atual: `npm run test --workspace @nuoma/api` passou com `app`, scheduler e push delivery; `npm run test --workspace @nuoma/db` passou com repositórios, FTS5 e claims.
- 2026-05-04 — V2.8 base visual reconciliada: `packages/ui` saiu da descricao/skin neumorphic e passou para Cartographic Operations com tokens OKLCH azul-esverdeados, surfaces flat/contour, micro-grid no shell/login/sidebar e uso seletivo de glass em overlays. Docs criados em `docs/design-system/`.
- 2026-05-04 — V2.8 shell mobile e push web avançados: sidebar ganhou atalhos 1-9/0 com navegação por teclado fora de inputs, drawer mobile via `Sheet`, service worker `/sw.js`, helper de subscription Web Push e aba Notificações em `/settings`. Aba Integrações adicionada na página de settings.
- 2026-05-04 — V2.8 motion e inventário fechados: `Animate`, `StaggerContainer`, `Button` e `SignalDot` respeitam `prefers-reduced-motion`. Docs `V2_COMPONENT_INVENTORY.md` e `V2_MOTION.md` criados.
- 2026-05-04 — V2.8 visual/a11y smoke fechado: script `npm run test:v28-visual` usa Playwright + axe em login, `/dev/components`, `/settings` notificações e drawer mobile, salvando screenshots em `data/v28-smoke-*.png`; execução atual passou com `violations=0` e `blocking=0` nas quatro telas.
- 2026-05-04 — V2.8 command palette integrada: `cmd/ctrl+k` normaliza tecla maiúscula, ações abrem fluxos seguros de criar contato, preparar campanha e testar automação por query `intent`; campanha/automação não disparam envio automaticamente. Browser smoke confirmou os três painéis.
- 2026-05-05 — V2.9.3 real-time Inbox fechado: `/api/inbox/events` emite `inbox-ready`, `message-added`, `heartbeat` e `stream-error` com CORS/cookie; `message-added` agora inclui `previousRank`/`nextRank`, payload de conversa e `pollMs`. Web aplica reorder otimista em `conversations.list({ limit: 200 })`, invalida a conversa ativa, mostra status visível de tempo real e tem smoke visual `npm run test:v29-inbox` com screenshot `data/v29-inbox-m0.png`. Testes: `npm run typecheck --workspace @nuoma/api`, `npm run typecheck --workspace @nuoma/web`, `npm test --workspace @nuoma/api -- app.test.ts`.
- 2026-05-05 — M0 melhorias futuras para o próximo round da Inbox: M0.1 trocar polling SSE por cursor/event bus persistido para emitir deltas por `message.id` sem depender de assinatura de conversa; M0.2 adicionar indicador de reconexão/backlog no cliente para mostrar quando o EventSource ficou offline e quantos eventos foram recuperados.
- 2026-05-04 — V2.9.3 real-time Inbox iniciado: API expõe `/api/inbox/events` em SSE autenticado por cookie, emite `inbox-ready`, `message-added`, `heartbeat` e `stream-error`; web consome com `EventSource` e invalida conversas/mensagens selecionadas. Browser smoke recebeu `inbox-ready` autenticado com 6 conversas.
- 2026-05-04 — Automation engine fechado: `apps/api` ganhou daemon `API_AUTOMATION_ENGINE_ENABLED` que começa no último `message.id`, varre inbound novas, avalia automações `message_received`, respeita `requireWithin24hWindow`, segmento por tag/status/canal, política de envio da API, aplica `apply_tag`/`remove_tag`/`set_status`/`create_reminder` e cria jobs `campaign_step` deduplicados por `messageId`.
- 2026-05-04 — Política produção de envio fechada: API ganhou `API_SEND_POLICY_MODE`/`API_SEND_ALLOWED_PHONES`; worker ganhou `WA_SEND_POLICY_MODE`/`WA_SEND_ALLOWED_PHONES`/`WA_SEND_RATE_LIMIT_*`. Modo `test` exige allowlist, modo `production` permite múltiplos telefones com allowlist canária opcional, todo envio permitido/bloqueado gera `sender.send_policy.allowed|blocked` e rate limit bloqueia antes de chamar o runtime WhatsApp.
